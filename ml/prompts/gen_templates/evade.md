Today's Wordle answer is {answer}. The server runs a spoiler filter and members are competing to sneak the answer past it.

Write examples for this answer as a JSON object with an "examples" array, one per style listed below. For direct examples, favour creative disguises: unusual separators, mixed emoji and letters, acrostics that read as a natural sentence, capitals hidden in a longer message, the word split across two words, the word respelled by sound and split across spaces. For hints, favour ones that look like ordinary chat to a moderator but are obvious to a solver. Benign examples should be the kind of message a filter is likely to get wrong.

{plan}
